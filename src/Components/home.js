import React, { Component } from "react";
import { Link } from "react-router-dom";

export default class Home extends Component {
  render() {
    return (
      <div>
        <h1 className="Center">Welcome to cloud hosted Website!</h1>
      </div>
    );
  }
}
